public class Bot extends ListenerAdapter {
    
    // Read from environment variable (secure)
    private static final String TOKEN = System.getenv("DISCORD_TOKEN");
    
    public static void main(String[] args) {
        if (TOKEN == MTQ4MjgxMjU3NjE5ODAzNzU5NQ.GKt9FI.Gg75Eoa76m57d2QSrYyHdK9Br5g84UKNEzV1-Q|| TOKEN.isEmpty()) {public class Bot extends ListenerAdapter {
    
    // Read from environment variable (secure)
    private static final String TOKEN = System.getenv("DISCORD_TOKEN");
    
    public static void main(String[] args) {
        if (TOKEN == null || TOKEN.isEmpty()) {
            System.err.println("ERROR: DISCORD_TOKEN not found in environment variables!");
            System.exit(1);
        }
        
        try {
            JDA jda = JDABuilder.createDefault(TOKEN).build();
            jda.addEventListener(new Bot());
            System.out.println("Bot is online!");
        } catch (LoginException e) {
            System.err.println("Invalid token provided");
            e.printStackTrace();
        }
    }
}
